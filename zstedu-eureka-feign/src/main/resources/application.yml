

server:
  port: 8762

spring:
  application:
    name: ApplicationEurekaFeign

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

feign:
  hystrix:
    enabled: true

